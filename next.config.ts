import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Il badge di sviluppo copre l'angolo della pagina durante le review visive.
  devIndicators: false,
};

export default nextConfig;
